# 4K 长视频自适应性能策略

YoloCut 始终保留并引用原始素材。剪辑预览使用可删除、可重建的 H.264
代理；最终导出仍读取原始素材，因此 540p/720p 代理不会降低 4K 成片画质。

## 自动档位

档位由实测逻辑线程、系统内存和 FFmpeg 单帧硬编码探测共同决定，不能仅根据
显卡名称推断。预览统一限制为 30fps，降低多小时素材的解码、显存和磁盘压力。

| 档位 | 触发条件 | 代理上限 | 目标码率 | 缓存上限 |
|---|---|---:|---:|---:|
| 低配流畅 | 不超过 4 线程、低于 12GB 内存，或无可工作硬编 | 960×540 | 2Mbps | 8GB |
| 均衡流畅 | 有硬编，但主机资源未达到高性能档 | 1280×720 | 4Mbps | 16GB |
| 高性能流畅 | 至少 8 线程、24GB 内存且硬编可工作 | 1920×1080 | 8Mbps | 32GB |

实际缓存还会保留 2GB 空闲磁盘，并且最多使用剩余空间的四分之一。预计代理
超出预算时拒绝生成，而不是写满磁盘。`MEDIA_PREVIEW_MAX_BYTES` 可由运维显式
覆盖。

## NVIDIA 路径

对于可用的 NVIDIA GPU，代理生成依次尝试：

1. NVDEC：`-hwaccel cuda -hwaccel_output_format cuda`；
2. CUDA 显存内缩放：`scale_cuda`；
3. NVENC：`h264_nvenc`；
4. 若源编码或驱动不支持，回退为硬解后复制 + NVENC；
5. 再回退为软解 + NVENC；
6. 最后才使用 `libx264`。

第一条路径不会强制软件像素格式，避免 FFmpeg 自动插入 CPU `scale` 而破坏
零拷贝。设置在预览区显示自动档位和代理高度，接口
`GET /api/media-performance-profile` 返回本次实际探测结果。

H.264 导出继续优先使用项目既有的 NVENC/QSV/AMF/VideoToolbox/VA-API 探测。
包含字幕、特效和多轨合成的导出必须经过渲染器合成，不能宣称为源视频到成片
的全程零拷贝；零拷贝保证针对的是 4K 剪辑代理转码。

## 长视频保护

- 代理任务单独串行，避免两段 4K 同时转码抢占 GPU、磁盘和内存。
- 客户端先处理当前时间线、再按片段出现顺序处理嵌套时间线；切换工程时尚未开始的无关任务会取消，不会把数小时素材一次塞满服务端队列。
- 普通 FFmpeg 工作线程约占逻辑核心的 60%，最多 12 线程，给界面和播放器留出余量。
- 即使组件因时间线更新暂时卸载，已经开始的代理仍在后台完成。
- 代理按素材指纹与性能档位缓存；源文件变更后不会误用旧代理。
- 客户端代理状态采用 256 项 LRU 上限；大型工程只复制真正改用代理的片段和时间线，避免每次代理完成都重建整份工程对象。
- 临时驱动、磁盘或编码失败五分钟后自动重试；已确认无法播放的坏代理不会循环重试。程序异常遗留的内部临时文件满 24 小时后才清理。
- 播放器按低配/均衡/高性能档分别预热约 0.25/0.5/1 秒，低配机器不会在每个切点长时间并行解码两路 4K。
- 同源长视频被切成大量片段时，共享音视频元素和音频闪避使用二分区间定位，不随切片数逐帧线性扫描。

## 验证

```powershell
npm run verify:media-performance
npm run verify:desktop-inference
npm run build
```

真实 NVIDIA 验收必须同时核对返回的 `encoder.id=h264_nvenc`、
`decoder.id=cuda`、`zeroCopy=true` 和输出文件的尺寸/时长。仅看到
`ffmpeg -hwaccels` 中列出 CUDA 不算运行验收。
