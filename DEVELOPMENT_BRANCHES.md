# 研发分支约定

本项目以 `codex/opencut-ui-agent` 作为集成分支。每次形成可运行、通过测试的基线后，三条研发分支从同一个提交点更新，避免前后端以不同协议各自演进。

## 分支职责

- `codex/frontend`：OpenCut 风格工作台、组件、样式、交互、响应式、可访问性与前端状态呈现。
- `codex/backend`：HTTP/MCP 路由、存储、密钥与权限、代理、桌面桥接、第三方 AI/ZCode 服务接入。
- `codex/features`：剪辑能力、Agent 工具、媒体工作流、导入/分析/导出以及需要前后端协同的新功能。

## 合并规则

1. 三条研发分支都向 `codex/opencut-ui-agent` 提交合并请求，不直接互相合并。
2. 共享协议放在 `shared/`，协议变更必须同时补齐服务端验证和前端验证。
3. 功能分支若需要 UI 或服务端改动，按可独立审查的提交拆分，并在同一合并请求中保持端到端可运行。
4. 合并前至少运行受影响验证、类型检查、构建与 lint；发布基线运行完整 `npm test`。
5. API Key、OAuth 凭据、ZCode 本地密钥、用户工程和测试导出文件不得提交。

## 当前基线

三条研发分支首次建立时都指向同一个已验证提交。后续差异只用于对应职责范围的研发，稳定成果回收到集成分支后再同步新基线。
